# @nfc/api

Backend API service (Shopify OAuth, webhook receivers, internal data API). **Implementation begins in Step 2 (Shopify OAuth Integration).** Step 1 reserves this workspace per PRD §8 Step 1.
